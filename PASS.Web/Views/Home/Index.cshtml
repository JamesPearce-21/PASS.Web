@{
    ViewData["Title"] = "Home";
}

@section Styles {
    <link rel="stylesheet" href="~/css/Home.css" asp-append-version="true" />
}

<!-- Hero Section (This was missing) -->
@await Html.PartialAsync("_Hero")

<!-- Main Calendar Section -->
@await Html.PartialAsync("Calendar")

<!-- Do not include Footer here (it is already in Layout) -->
